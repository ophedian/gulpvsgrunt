var calculator = calculator || {};

(function (calc) {
    calc.substract = function (x, y) {
        return x - y;
    };

    calc.add = function (x, y) {
        return x + y;
    };

}(calculator));